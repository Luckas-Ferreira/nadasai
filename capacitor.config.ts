import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nadasai.app',
  appName: 'Nada Sai',
  webDir: 'dist/nadasai-android/browser'
};

export default config;
