/**
 * A versão ANDROID de `platform.ts`, trocada por `fileReplacements` na
 * configuração `android` do `angular.json`. Leia o original: ele é quem explica
 * a regra.
 */
export const PACKAGED = true;
